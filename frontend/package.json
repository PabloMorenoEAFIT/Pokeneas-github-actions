{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "serve": "vite --host",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start:all": "concurrently \"npm run dev\" \"cd ../backend && node server.js\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "vite": "^5.4.10"
  },
  "dependencies": {
    "page": "^1.3.7"
  }
}
